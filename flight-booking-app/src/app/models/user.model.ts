export interface User {
    userId: string; // Assuming this is a GUID or string identifier
    uName: string;
    email: string;
    phone: string;
    dateOfBirth: string; // ISO date string (YYYY-MM-DD)
    username: string;
    password: string;
  }
  